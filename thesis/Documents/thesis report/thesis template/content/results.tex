% !TEX root = ../thesis.tex

\chapter{Results}
\label{ch:results}

% Research-question driven structure: each top-level section directly answers
% one of the three research questions stated in §\ref{sec:intro:rqs}. All
% numerical findings live in this chapter; metric definitions remain in
% Chapter~\ref{ch:evaluation}.

\section{RQ1 -- Methods and Metrics for Evaluating Synthetic Medical Data}
\label{sec:results:rq1}

% RQ1 (verbatim): "Which methods and metrics are used to evaluate privacy
% risks and utility (including predictive performance and distributional
% resemblance) in synthetic medical data?"
%
% This RQ has both a literature-review answer (Chapter 2.6) and an empirical
% answer: of all the metrics implemented, which ones actually discriminated
% between the four generators on the Diabetes 130-US benchmark? Surface that
% empirical answer here.

\subsection{Operationalising the Two-Pillar Framework}
\label{sec:results:rq1:operationalising}

% TODO: Brief recap of the 17-metric framework operationalised on Diabetes
% 130-US Hospitals. Cross-reference §5.

\subsection{Per-Pillar Diagnostic Value}
\label{sec:results:rq1:diagnostic}

% TODO: For each metric, report:
%  - Range observed across generators (min, max).
%  - Whether the metric ranked the generators consistently with the others.
%  - Identify metrics that collapsed (e.g., all 4 generators got near-perfect
%    AUC) vs. metrics that meaningfully separated them.
% Recommended: a table titled "Discriminative power of each evaluation
% metric" with one row per metric and columns for (range, separates DP vs.
% non-DP?, separates GAN vs. LLM?). Group rows by sub-branch:
%   (a) Utility / statistical similarity
%   (b) Utility / predictive performance
%   (c) Privacy

\subsection{Recommended Minimal Evaluation Suite}
\label{sec:results:rq1:minimal-suite}

% TODO: Based on §6.1.2, propose a minimal set (e.g., KS, TSTR-AUC, NNDR,
% MIA Advantage) that covers both pillars without redundancy.

\subsection*{Answer to RQ1}
\label{sec:results:rq1:answer}

% TODO: One-paragraph synthesis. Example structure:
%  - Surveying the literature (§2.6) yields two pillars (utility and
%    privacy) and ~20 candidate metrics; this thesis instantiates 17 of them.
%  - Empirically, the most diagnostic metrics on Diabetes 130-US Hospitals
%    are: <list>. Less diagnostic: <list>.
%  - Recommended minimal suite for medical SDG evaluation: <list>.

% -------------------------------------------------------------------

\section{RQ2 -- Utility--Privacy Trade-offs Per Generator}
\label{sec:results:rq2}

% RQ2: "What are the trade-offs in utility and privacy in GAN- and LLM-based
% synthesised data generation models?"

\subsection{Utility Results}
\label{sec:results:rq2:utility}

% Utility is reported in two complementary blocks: statistical similarity
% (Section~\ref{sec:eval:utility:similarity}, notebook cells 29–34) and
% predictive performance (Section~\ref{sec:eval:utility:predictive}, cells
% 35–41).

\subsubsection{Statistical Similarity}
\label{sec:results:rq2:utility:similarity}

% Notebook cells 29–34. Per-generator findings:

\paragraph{Per-Column Distributions and Marginals.}
% TODO: Summarise marginal-match quality. Headline figure: a 4×N grid of
% selected columns × generator (or supplementary appendix).

\paragraph{Pairwise Correlation Difference (PCD).}
% TODO: Report per-generator scalar PCD (Frobenius norm of the correlation
% matrix difference). Add column-level heatmaps in Appendix.

\paragraph{Kolmogorov--Smirnov Statistics.}
% TODO: Mean KS per generator + boxplot across columns. Identify columns
% with the largest distributional drift per generator.

\paragraph{PCA-Based Manifold Overlap.}
% TODO: Single 2D PCA plot with all four synthetic clouds + real points.
% Discuss which generator achieves the broadest overlap.

\subsubsection{Predictive Performance}
\label{sec:results:rq2:utility:predictive}

% Notebook cells 35–41. Per-generator findings:

\paragraph{TSTR Classifier Performance.}
% TODO: Master table — Train Source × Classifier with AUC, F1, Accuracy,
% Precision, Recall. Highlight the gap to the TRTR baseline.

\subsubsection{Ablation: Age Binning Granularity}
\label{sec:results:rq2:utility:age-binning}

The preprocessing pipeline (\S\ref{sec:data:fe:age}) retains the dataset's
native ten-decade encoding of \texttt{age} after an earlier design that
collapsed it into three coarse groups (young / middle-aged / elderly) was
abandoned. Table~\ref{tab:age-binning-tstr} quantifies the impact on
Train-on-Synthetic, Test-on-Real (TSTR) utility, evaluated on the real
held-out test split ($n = 14{,}304$) with \texttt{readmitted} as the
binary target.

The Pythia LLM generator improves substantially under the ten-bin encoding
across all three classifiers: AUC-ROC rises by $0.044$--$0.067$ and
macro-F1 by $0.099$--$0.112$. The Train-on-Real upper bound is essentially
unchanged between the two preprocessing variants (Gradient-Boosting TRTR
AUC: $0.7014$ under the three-bin scheme vs.\ $0.7008$ under the ten-bin
scheme), confirming that the gain originates from improved synthetic
generation rather than a shift in the real-data signal. Scaling Pythia from
70M to 1B parameters under the ten-bin encoding further raises
Gradient-Boosting TSTR AUC to $0.6818$, recovering $\approx 97.3\%$ of the
TRTR upper bound.

\begin{table}[h]
\centering
\small
\caption{TSTR utility for the Pythia LLM generator under the discarded
three-bin age encoding versus the retained ten-bin encoding. Test set: real
held-out split ($n = 14{,}304$); target: \texttt{readmitted}.
$\Delta$ reports the change from three-bin to ten-bin (Pythia-70M).}
\label{tab:age-binning-tstr}
\begin{tabular}{llcccc}
\toprule
Classifier & Metric & 3-bin (Pythia-70M) & 10-bin (Pythia-70M) & $\Delta$ & 10-bin (Pythia-1B) \\
\midrule
Logistic Regression & AUC-ROC & 0.6128 & 0.6570 & $+0.0442$ & 0.6612 \\
Logistic Regression & F1      & 0.4046 & 0.5038 & $+0.0992$ & 0.4995 \\
Random Forest       & AUC-ROC & 0.5917 & 0.6588 & $+0.0671$ & 0.6682 \\
Random Forest       & F1      & 0.3843 & 0.4966 & $+0.1123$ & 0.5048 \\
Gradient Boosting   & AUC-ROC & 0.5981 & 0.6634 & $+0.0653$ & \textbf{0.6818} \\
Gradient Boosting   & F1      & 0.3944 & 0.5005 & $+0.1061$ & 0.5036 \\
\midrule
\multicolumn{2}{l}{TRTR (real) Gradient-Boosting AUC} & 0.7014 & 0.7008 & --- & --- \\
\bottomrule
\end{tabular}
\end{table}

A plausible mechanistic explanation is that Pythia emits each field as a
discrete token: under the three-bin scheme, three tokens absorbed the entire
0--100 year range, erasing conditional structure between age and clinically
correlated features (\texttt{number\_diagnoses},
\texttt{number\_inpatient}, \texttt{insulin}). Restoring the ten-decade
encoding provides a finer conditioning vocabulary, recovering age-specific
co-occurrence patterns relevant to readmission prediction. The PATE-GAN and
HealthGAN baselines did not exhibit a comparable sensitivity to this
preprocessing choice, suggesting that categorical cardinality affects
token-based LLM synthesisers more than GAN-based ones on tabular medical
data. This observation is discussed further in
Section~\ref{sec:discussion:gan-vs-llm}.

\paragraph{ROC Curves (Gradient Boosting).}
% TODO: Single overlay plot — Real (TRTR) + 4 synthetic ROC curves.

\paragraph{Feature Importance Stability.}
% TODO: Spearman ρ between Real RF importances and each generator's RF
% importances. Identify any feature whose ranking flips dramatically.

\paragraph{Cox Proportional Hazards.}
% TODO: HR table per generator vs. Real. Forest plot.

\paragraph{Cox CI Overlap.}
% TODO: Bar chart — fraction of covariates with overlapping CIs.

\subsection{Privacy Results}
\label{sec:results:rq2:privacy}

% Notebook cells 42–48. Per-generator findings:

\subsubsection{Nearest-Neighbour Distance and NNDR}
% TODO: NNDR table per generator + boxplot of synth→real NN distances.

\subsubsection{Nearest-Neighbour Adversarial Accuracy}
% TODO: TrainAA, TestAA, Privacy Loss per generator (target loss ≈ 0).

\subsubsection{Membership Inference Attack}
% TODO: MIA AUC and MIA Advantage per generator.

\subsubsection{Attribute Inference Attack}
% TODO: Per-attribute advantage table (rows: readmitted, gender, age,
% diag_1; columns: generators). Headline metric = max advantage.

\subsubsection{Outlier Linkability}
% TODO: Fraction of synthetic neighbours within radius of real outliers.

\subsection{Within-Family DP Cost}
\label{sec:results:rq2:dp-cost}

\subsubsection{HealthGAN $\to$ PATE-GAN}
% TODO: Side-by-side comparison.
%  - Utility delta (similarity branch): KS / PCD / PCA changes.
%  - Utility delta (predictive branch): AUC drop, F1 drop, Cox CI overlap drop.
%  - Privacy gain: NNDR change, MIA Advantage change, AIA Advantage change.
%  - Net assessment: is the utility sacrifice "paid for" by privacy gains?

\subsubsection{Pythia $\to$ Pythia-DP}
% TODO: Same structure as above, for the LLM family. Note the achieved
% epsilon from run_metadata_dp.json.

\subsection*{Answer to RQ2}
\label{sec:results:rq2:answer}

% TODO: One paragraph per generator family, synthesising each model's
% tradeoff position. Example skeleton (utility = similarity + predictive):
%  - HealthGAN:  high similarity, high predictive, MODERATE privacy (no DP).
%  - PATE-GAN:   lower utility on both branches, higher privacy, formal DP.
%  - Pythia:     <similarity / predictive / privacy summary>.
%  - Pythia-DP:  <similarity / predictive / privacy summary>.

% -------------------------------------------------------------------

\section{RQ3 -- Which Approach Achieves Better Balance}
\label{sec:results:rq3}

% RQ3: "Which synthetic data generation approach, based on GANs or Large
% Language Models, achieves a more effective balance between data utility
% and privacy preservation when applied to medical datasets?"

\subsection{Tradeoff Landscape}
\label{sec:results:rq3:tradeoff}

% Notebook cell 50.
% TODO: Privacy–Utility scatter plot (NNDR vs. Gradient-Boosting AUC) with
% all four generators + Real (TRTR) reference. Identify the upper-right
% quadrant occupants.

\subsection{Six-Dimension Radar Comparison}
\label{sec:results:rq3:radar}

% Notebook cell 51.
% TODO: Radar chart with the four generators overlaid; describe the shape
% of each polygon and which generator dominates which axes (three utility
% axes — similarity, predictive, Cox CI overlap — and three privacy axes —
% NN, MIA, AIA).

\subsection{Comprehensive Summary Table}
\label{sec:results:rq3:table}

% Notebook cell 52.
% TODO: 18-metric × 5-source comprehensive table, columns grouped by
% pillar/branch (Utility-similarity, Utility-predictive, Privacy). Highlight
% the best entry per column (excluding the Real reference).

\subsection{Pareto Analysis: GAN Family vs. LLM Family}
\label{sec:results:rq3:pareto}

% TODO: Identify Pareto-optimal generators (not dominated on (utility,
% privacy)). Discuss which family contributes more Pareto-optimal points.

\subsection*{Answer to RQ3}
\label{sec:results:rq3:answer}

% TODO: Conditional recommendation in 3–4 sentences:
%  - If formal DP is required AND similarity-sensitive downstream task →
%    <generator>.
%  - If formal DP is required AND tabular-rich data structure → <generator>.
%  - If informal privacy is acceptable AND maximum utility → <generator>.
% Make explicit which family "wins" in your specific empirical setting.

\cleardoublepage
